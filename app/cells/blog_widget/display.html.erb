<!--div class="widget-blog">
  <script src="http://widgets.twimg.com/j/2/widget.js"></script>
    <script>
    new TWTR.Widget({
      version: 2,
      type: 'profile',
      rpp: 6,
      interval: 6000,
      width: 201,
      height: 300,
      theme: {
        shell: {
          background: '#aaaaaa',
          color: '#1f0c1f'
        },
        tweets: {
          background: '#ffffff',
          color: '#666666',
          links: '#003e74'
        }
      },
      features: {
        scrollbar: true,
        loop: false,
        live: true,
        hashtags: true,
        timestamp: true,
        avatars: false,
        behavior: 'all'
      }
    }).render().setUser('kabcast').start();
    </script>
</div-->
<%- unless @feed.empty? -%>
<div class="widget-blog">
  <h4><%= link_to t('home.views.blog_title').html_safe, t('home.views.blog'), :class => 'in-wide-iframe' %></h4>
  <hr/>
  <ul>
    <% @feed[0..10].each { |item| %>
        <li>
          <%= link_to item.title, item.url, :class => @link_class %>
          <span><%= t("stream.posted_ago", :when => time_ago_in_words(item.published)) %></span>

        </li>
    <% } %>
  </ul>
  <%= link_to t("stream.read_all"), t('home.views.blog'), :class => 'more_link in-wide-iframe' %>
</div>
<%- end -%>
