<%- the_only_widget = (!@stream_preset.show_questions && !@stream_preset.show_schedule && !@stream_preset.show_sketches && !@stream_preset.show_coveritlive) ? 'display: block;' : '' -%>
<%= widget_div(:style => the_only_widget) do -%>
    <%= @stream_preset.special_schedule.html_safe %>
<%- end -%>
