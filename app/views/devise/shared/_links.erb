<%#- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <%= link_to t('devise.views.no_confirmation_instructions'), new_confirmation_path(@resource_name), :class => [:fleft, :'small-text'] %><br />
<%# end -%>

<%#- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to t('devise.views.no_unlock_instructions'), new_unlock_path(@resource_name), :class => [:fleft, :'small-text'] %><br />
<%# end -%>
