  <%= t('devise.mails.welcome') %>, <%= "#{@resource.first_name} #{@resource.last_name}"%>

  <%= t 'devise.mails.confirm_through_link' %>:

  <%= link_to t('devise.mails.confirm_my_account'), user_confirmation_url(:user => @resource, :locale => locale, :confirmation_token => @resource.confirmation_token) %>
