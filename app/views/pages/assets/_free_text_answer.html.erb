<% if question.is_multiline %>
  <%= builder.text_area :text_value, :rows => 5, :cols => 80, :class => 'free_text required', :title => 'required field' %>
<% else %>
  <%= builder.text_field :text_value, :class => 'free_text required', :title => t('pages.views.required') %>
<% end %>
