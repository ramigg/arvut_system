<% if current_user.is_restricted? %>
  <%#= statistics(new_statistic_url, 'stats', 'reg-vs-button', 600, 300, @start, @finish) %>
  <dl id="admin-menu">
    <% if current_user.is_admin? || current_user.is_super_moderator? || current_user.is_moderator? %>
      <dt><%= t('admin.reports.manage_questionnaires') %>:</dt>
      <dd><%= link_to t('admin.reports.pages_list'), admin_pages_url %></dd>
    <% end %>
    <% if current_user.is_admin? || current_user.is_reports? %>
      <dt><%= t('admin.reports.reports') %>:</dt>
      <dd><%= link_to t('admin.reports.attendance_report'), admin_attendance_reports_url %></dd>
      <dd><%= link_to t('admin.reports.basic_report'), admin_basic_reports_url %></dd>
      <dd><%= link_to t('admin.reports.questionnaire_report'), admin_questionnaire_reports_url %></dd>
    <% end %>
    <% if current_user.is_admin? || current_user.is_groupmanager? %>
      <dt><%= t('admin.reports.user_management') %>:</dt>
      <dd><%= link_to t('admin.reports.manage_user_roles'), admin_roles_url %></dd>
        <dd><%= link_to t('devise.views.new_user'), new_admin_user_url %></dd>
      <dd><%= link_to t('admin.reports.remove_user'), admin_remove_user_url %></dd>
    <% end %>
    <% if current_user.is_admin? || current_user.is_groupmanager? %>
      <dt><%= t('admin.reports.group_management') %>:</dt>
      <dd><%= link_to t('admin.reports.manage_user_lists'), admin_user_lists_url %></dd>
      <dd><%= link_to t('admin.reports.load_group_list'), new_admin_users_group_url %></dd>
    <% end %>
    <% if current_user.is_admin? || current_user.is_stream_manager? %>
      <dt><%= t('kabtv.admin.stream_management') %>:</dt>
      <dd><%= link_to t('kabtv.admin.stream_management'), admin_stream_management_url %></dd>
    <% end %>
    <% if current_user.is_super? %>
      <dt>Cache Management:</dt>
      <dd><%= link_to 'Clear Cache', admin_clear_cache_url %></dd>
    <% end %>
    <% if current_user.is_translator? || current_user.is_admin? || current_user.is_super? %>
      <dt><%= t('admin.reports.translations') %>:</dt>
      <dd><%= link_to t('admin.reports.translations'), root_tolk_url %></dd>
    <% end %>
  </dl>
<% end %>
