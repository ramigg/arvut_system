<%= form_for @user_list, :html => {:id => 'user_list'} do |builder| %>
  <%= render :partial => 'form', :locals => {:builder => builder, :submit_text => t('admin.user_list.update_user_list')} %>
<% end %>