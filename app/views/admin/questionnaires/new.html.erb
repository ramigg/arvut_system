<%= form_for @questionnaire, :html => {:id => 'questionnaire'} do |f|%>
  <%= render :partial => 'form', :locals => { :builder => f, :submit_text => t('admin.questionnaires.create_questionnaire') } %>
<% end %>