<%= form_for :admin_questionnaire, @questionnaire, :html => {:id => 'questionnaire'} do |f|%>
  <%= render :partial => 'form', :locals => { :builder => f, :submit_text => t('admin.questionnaires.update_questionnaire') } %>
<% end %>